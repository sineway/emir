{
	"devDependencies": {
		"jasmine-core": "3.7.1"
	},
	"scripts": {
		"build": "zip 'dist' --filesync --recurse-paths './' --include '_locales/*' 'api/*' 'ui/*' 'background.*' 'defaults.js' 'estimates.*' 'manifest.json' 'settings.*' --exclude 'api/test/*'"
	}
}
